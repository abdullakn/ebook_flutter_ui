import 'package:flutter/material.dart';

class BookStaggeredGridView extends StatelessWidget {
  const BookStaggeredGridView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}